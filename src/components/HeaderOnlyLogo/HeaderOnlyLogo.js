import './HeaderOnlyLogo.css';
import Logo from '../Logo/Logo';

const HeaderOnlyLogo = () => {
  return (
    <header className="HeaderOnlyLogo">
      <Logo />
    </header>
  );
};

export default HeaderOnlyLogo;
